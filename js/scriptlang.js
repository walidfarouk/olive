Headers
import React from 'react'
import PropTypes from 'prop-types'
import { connect } from 'react-redux'

export const mapStateToProps = state => ({

})

export const mapDispatchToProps = {
 
}

export const 
d= (WrappedComponent) => {
    const hocComponent = ({ ...props }) => <WrappedComponent {...props} />

    hocComponent.propTypes = {
    }

    return hocComponent
}

export default WrapperComponent => connect(mapStateToProps, mapDispatchToProps)(
    d
    
(WrapperComponent)const [propertyName] = walid farouk
;
false     walid 
)departFocus  (lido didoo 
    )

    finally  my navigator localStorage
    
const [propertyName] = walid farouk
;
false     walid 
departFocus  (lido didoo 
    )

    finally  my navigator localStorage
    